Interrupted before completion. These partial runs used explicit foreach=False.
They are excluded from all formal summaries. The completed formal protocol
uses the same default torch.optim.Adam execution path as Exp1.
